# Simple chatbot

This is the code for the tutorial that can be found [here](TODO).

Run `npm install` or `yarn` to install the packages required.

To build the CSS required to run the application execute `npm run build` or 
`yarn run build`.

To start a server run:

- `npm run start-v1`/`yarn run start-v1` to run the version 1;
- `npm run start-v2`/`yarn run start-v2` to run the version 2.